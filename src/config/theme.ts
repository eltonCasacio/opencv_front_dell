import { createTheme } from "@mui/material";

export const appTheme = createTheme({
    palette: {
        mode: 'dark',
        primary: { main: "#f5f5f1" },
        secondary: { main: "#1E90FF" },
        text: { primary: "#f5f5f1" },
    },
})